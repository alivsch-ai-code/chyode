# Shows a joke warning banner to anyone attempting to log in as a user other
# than our real accounts, before any password prompt. Real users are unaffected.
Match User *,!root,!azamat,!claude
    Banner /etc/ssh/troll_banner.txt
